apiVersion: v1
kind: Service
metadata:
  name: redbus-backend
  namespace: default
spec:
  # Internal only: the frontend nginx proxies /v1/api to this service.
  type: ClusterIP
  selector:
    app: redbus-backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
