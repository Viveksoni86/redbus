apiVersion: v1
kind: Service
metadata:
  name: redbus-backend
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: redbus-backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
